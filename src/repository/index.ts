import * as booksRepository from "./books"
import * as loanRepository from "./loans"
import * as authorRepository from "./authors"
import * as userRepository from "./users"

const repository = {
 booksRepository,
 loanRepository,
 authorRepository,
 userRepository

}

export default repository